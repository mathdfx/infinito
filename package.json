{
  "name": "ticket-demo",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "db:push": "bun run --cwd packages/db push",
    "db:generate": "bun run --cwd packages/db generate",
    "db:migrate": "bun run --cwd packages/db migrate",
    "db:seed": "bun run --cwd packages/db seed",
    "test:concurrency": "bun --env-file=apps/api/.env apps/api/src/concurrency-test.ts",
    "test:pay-race": "bun --env-file=apps/api/.env apps/api/src/pay-race-test.ts"
  },
  "devDependencies": {
    "oxlint": "^1.69.0",
    "turbo": "^2",
    "typescript": "^5.7"
  }
}
